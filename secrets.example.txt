# Template for CI / operator secrets — store real values in GitHub Secrets or a password manager.
# Do NOT commit a filled secrets.txt.

VPS_HOST=
VPS_PORT=22
VPS_USER=root
SSH_PRIVATE_KEY=

DOCKER_USERNAME=
DOCKERHUB_TOKEN=

# Optional notes:
# - JWT_KEY and POSTGRES_PASSWORD live in VPS /opt/shop/.env (candyRose), not in this repo.
# - After any leak, rotate JWT_KEY on the VPS and force users to re-login.
